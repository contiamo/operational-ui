{
  "name": "operational-ui",
  "version": "0.1.0",
  "description": "Building blocks for effective operational user interfaces",
  "repository": "git@github.com:Contiamo/operational-ui.git",
  "private": true,
  "author": "Tejas Kumar <tejas@contiamo.com>",
  "license": "MIT",
  "scripts": {
    "start": "cd packages/website && yarn start",
    "build": "run-s build:*",
    "build:clean": "rimraf dist",
    "build:website": "cd packages/website && yarn build && mv dist ../..",
    "build:visual-test":
      "cd packages/visual-tests && yarn build && mv dist ../../dist/visual-tests",
    "build:components":
      "cd packages/components && yarn docs:build && cp -r styleguide ../../dist/docs",
    "bootstrap": "lerna bootstrap",
    "postinstall": "npm run bootstrap",
    "clean":
      "rm -rf node_modules && rm -rf packages/*/node_modules && rm -f packages/*/package-lock.json && rm -f packages/*/yarn.lock && rm -f yarn.lock",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "prettier": "lerna run prettier",
    "precommit": "lerna run lint && lerna run prettier && git add .",
    "package": "lerna run package",
    "prepublish": "lerna run package",
    "publish": "lerna publish",
    "next": "./scripts/next.sh",
    "ensure:clean:repo": "./scripts/ensure-clean-repo.sh",
    "verify":
      "lerna run package && lerna run test && lerna run lint && lerna run prettier",
    "ci": "./scripts/ci.sh",
    "deploy:website": "./scripts/deploy-website.sh"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.7.1",
    "npm-run-all": "^4.1.3",
    "rimraf": "^2.6.2"
  },
  "workspaces": ["packages/*"]
}
