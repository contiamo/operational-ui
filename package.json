{
  "name": "contiamo-ui",
  "version": "0.0.0",
  "description": "An all-encompassing framework of UI components, composed components, and a showcase of components that compose together to create Contiamo products.",
  "repository": "git@github.com:Contiamo/ui-components.git",
  "private": true,
  "author": "Tejas Kumar <tejas@contiamo.com>",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "postbootstrap": "node ./scripts/postbootstrap",
    "postinstall": "node ./scripts/postinstall",
    "start": "cd packages/showcase && npm run start",
    "package": "lerna run package",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "prettier": "lerna run prettier",
    "precommit": "npm run test && npm run lint && npm run prettier && git add .",
    "preversion": "lerna run version && git add ."
  },
  "devDependencies": {
    "chalk": "^2.1.0",
    "husky": "^0.14.3",
    "lerna": "^2.1.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
