Tables simply render a semantic HTML table structure based on raw data.

### Usage

```js
<Table 
  columns={[ "Name", "Title"]}
  rows={[
    [ "Max", "Carpenter" ],
    [ "Moritz", "Baker" ]
  ]}
/>
```
