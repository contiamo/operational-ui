import * as React from "react"
import glamorous from "glamorous"
import withLabel from "./utils/with-label"

export interface IProps {}

const Checkbox = (props: IProps) => <div />

export default withLabel(Checkbox)
