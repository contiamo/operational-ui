import { IMarathon } from "../../../components/Marathon/Marathon"

const marathon = ({ test, container }: IMarathon): void => {
  test("One", () => {})

  test("Two", () => {})

  test("Three", () => {})
}

export default marathon
