const chalk = require("chalk")
const { log } = console

log(chalk.yellow.bold("👌🏾  Almost done!"))
log(chalk.black.bold("👢  Run `yarn bootstrap` or `npm run bootstrap` now to link the packages together."))
